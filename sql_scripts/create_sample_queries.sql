CREATE TABLE SAMPLE_QUERIES (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY, -- Auto-incrementing primary key
    table_name VARCHAR2(100) NOT NULL,          -- Name of the table (up to 100 characters)
    sample_query VARCHAR2(4000) NOT NULL,       -- Sample query (up to 4000 characters)
    CONSTRAINT sample_queries_pk PRIMARY KEY (id)
);
